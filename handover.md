# HANDOVER aitaxadvisors

Sliding session log. Newest entry first. Update at end of every session.

## Current State (07/20/2026, session 12)
- Screenshots showed the session-11 footer fix was still broken: the "Get your taxes right, sooner." heading was rendering huge and clipped/overlapping content above it. Root cause: `thailand/*.html` pages already have a correct, working version of this exact footer (predates this whole multi-session effort), and it works via a completely different mechanism than what got hand-built in session 11: it loads `css/site.css` directly as a 4th stylesheet (after `thailand.css`, so it wins the cascade), wraps only the `<footer>` in a `.site-scope` div so `--site-*` vars resolve, uses real `<a class="btn btn-light">`/`<a class="btn btn-outline-dark">` anchor buttons inside `.hero-ctas`, `data-reveal` attributes (not `class="reveal"`), and loads `js/site.js`. Session 11's hand-ported CSS copy into `css/thailand.css` kept silently drifting from the real values (this is the second size-regression bug found there, after the heading-size one already fixed). Removed the ported CSS entirely, switched all 10 `australia/*.html` pages to the verified thailand pattern, and localized the `Call` row to Australia's real number (+61 2 8007 4203, Sydney) since Thailand's own footer turns out to already do per-country localization (reversing the "generic HK everywhere" call made in session 11 round 2, which was a guess made without the reference implementation in hand). `.md/page-remediation-playbook.md` rewritten to document the verified pattern, including a note about the wrong attempts that preceded it, so the next 10 countries don't repeat them. One commit pushed to `main`.
- **Lesson for next time**: before hand-building or porting anything that "should already exist somewhere," check whether a working reference implementation already exists in the codebase first (in this case, Thailand's own pages had it the whole time).

## Current State (07/19/2026, session 11 continued)
- User feedback on the session 11 australia work: body images too small, all 10 pages sharing one hero image, footer missing the homepage's contact section, fonts looked off in that area. Root cause on the footer/font complaint: the homepage's `<section class="contact" id="contact">` block is styled by `css/site.css`, which country pages never load (loading it directly was rejected — its unscoped `.container { max-width: 1400px }` would've clobbered `global.css`'s `.container` sitewide). Fixed by porting just the needed `.contact`/`.contact-grid`/`.contact-details`/`.display`/`.accent` rules into `css/thailand.css` on thailand's own navy/blue vars, then adding the actual contact section markup (headline, CTA buttons, office hours, `<dl>` details) as the first child of every page's `<footer id="footer">`, before `.footer-top`. Also: widened `#hero`'s shared grid (`css/thailand.css`, `30fr` → `40fr` for hero-right, affects every country including Thailand), resized every body-content image from ~250-270px to 560-720px (40-80% bigger than the specialist section's ~400px image, per instruction, using a new shared `.article-body-img` class), and gave all 10 pages unique hero images (previously all 10 shared one file). `.md/page-remediation-playbook.md` updated with all of this as new standing sections (2b/2e hero width+uniqueness, 4a-2 contact section, 9b body image sizing) so it carries forward to the other 10 countries. One commit pushed to `main`.

## Current State (07/19/2026, session 11)
- First country from `.md/page-remediation-playbook.md`'s follow-up list done: all 10 `australia/*.html` pages remediated (hero image hack removed, clearfix + `.article-checklist` grid CSS added, "When to Consult a Specialist" section added to the 9 pages missing it, hub page's pre-existing specialist section converted from flex to the grid pattern, `callout-box padding: 0.8rem 160px` mobile-squeeze bug fixed on the 2026 checklist page's 11 items). No preloader, FAQ corruption, or duplicate-image issues found on this country, unlike Thailand. Deliberately did **not** touch footer contact info (playbook 4c) per standing instruction not to bulk-replace the generic HK head-office framing without being asked. One commit pushed to `main`. **Same known limitation as session 10: no browser tool available, verified via tag-balance/grep only, not visually confirmed.**
- Remaining countries still needing the same sweep: `cambodia/`, `indonesia/`, `japan/`, `malaysia/`, `oman/`, `philippines/`, `saudi-arabia/`, `taiwan/`, `uae/`, `vietnam/`. User wants these run one country at a time with review checkpoints between each, not all at once.

## Current State (07/19/2026, session 10)
- Full remediation pass on all 13 `thailand/*.html` pages (`base-template.html` included where relevant), surfacing and fixing a long list of real bugs that were present on nearly every page because they came from the same earlier batch-generation process. This is very likely to recur on the ~130 other pages across every other country folder, since they were built the same way. **A new companion playbook exists specifically for this: `.md/page-remediation-playbook.md`** (see Section below and the doc itself for full detail). Read it before touching any other country's existing pages.
- Homepage rebuilt: `homepage.html` promoted to `index.html` (old homepage backed up, then the backup was separately cleaned up outside this session). `css/holistic-style.css` / `js/holistic-style.js` renamed to `css/site.css` / `js/site.js`; every `.hp-*` class / `--hp-*` variable / `#hp-*` anchor renamed to `.site-*` / `--site-*` / `#site-*` sitewide (13 Thailand pages + hub + template). No more "holistic"/"hp-" trace anywhere in the codebase.
- Preloader restricted to `index.html` only (was present, and playing, on every single page). Removing it exposed a second, more serious bug: `animateHero()` (which reveals the hero AND every `.reveal` element sitewide via `initScrollAnimations()`) was only ever called from inside the now-dead `if (preloader)` branch in `js/global.js`, so removing the div made the entire rest of every page's content permanently invisible. Fixed by making the reveal call run unconditionally on page load regardless of whether a preloader element exists. This is a load-bearing fix, don't reintroduce a preloader-gated reveal call anywhere.
- Fixed a corrupted-FAQ-markup bug (mismatched/duplicated `<div class="article-faq-list">` opens, up to 6 stray opens in one section on 4 pages) left over from an even earlier batch edit, plus 3 pages carrying a duplicated generic FAQ block on top of their own page-specific one.
- Standardized the Key Topics section (same 8 links, dark background) and added a "When to Consult a Specialist" section (unique photo + 5-item checklist) and a unique topic-specific mid-page CTA to every Thailand page; several were missing one or both entirely.
- Rebuilt hero image handling: replaced the fragile per-image `transform: translateX(-80px) scale(1.4)` hack (broke badly on portrait-oriented photos, and caused a title/image overlap bug on at least one page) with a single shared CSS rule (`.hero-right img { height: 560px; max-height: 70vh; object-fit: cover; }` in `css/thailand.css`) that handles any aspect ratio via cropping. Every hero `<img>`'s inline style is now just `border-radius` + `box-shadow`, nothing else.
- Body content images: removed several duplicate-image-on-one-page cases, fixed multiple wrong-subject photos (US 1040 forms on a Thai-filing page, generic tax forms on a FAFSA/student page), added a section-level clearfix (`.article-section .container::after { clear: both }`) so floated images can't bleed into the next section, and fixed a subtler bug where a background-styled callout box sitting directly after a float (with no clearing) visually collided with it.
- Fixed the "When to Consult a Specialist" checklist's icon/label/description alignment: it was `display: flex` (label and description as separate flex siblings, so a two-line label pushed the description to a different x-position every row); switched to explicit CSS Grid placement, fixed regardless of label length.
- Corrected a factual error: DTV is the **Destination Thailand Visa**, not "Digital Nomad Visa" (that's just the common nickname), corrected wherever it had been stated as the official name.
- `md/` folder renamed to `.md/` (hidden), `.gitignore` and every reference across `README.md`, `handover.md`, `robots.txt`, `.claude/settings.local.json`, and the two playbook docs updated to match.
- **Known limitation, be upfront about it**: everything above was verified via tag-balance checks, grep, and static file review, not a live browser render (Chrome tool was disconnected all session). Visually confirm on at least a couple of pages before assuming a fix "looks right," especially anything float/hero-related.

## Current State (07/15/2026, session 9)
- 12th country added: `japan/` (10 pages: hub, filing, FEIE vs FTC, tax treaty & totalization, Non-Permanent Resident 5-year rule, retiring, checklist, teachers (JET/ALT/eikaiwa), property ownership, HSP & digital nomad visa). Japan is the standout in our entire coverage on two fronts: it's the only country with BOTH a real, active income tax treaty AND a real Totalization Agreement (most countries have one or neither), and its combined national+local+surtax rate reaches ~55.945% at the top, the highest of any country covered, higher than Australia (45%) or Taiwan (40%), making FEIE vs FTC modeling matter more here than anywhere else. The standout distinctive angle is the Non-Permanent Resident 5-year rule: a genuine, well-documented three-tier residency system (non-resident/non-permanent resident/permanent resident) where unremitted foreign income escapes Japanese tax entirely for your first 5 of any rolling 10 years, broader and more established than Indonesia's narrow 4-year foreign-skill carve-out. Property ownership has zero restrictions (genuine freehold, no residency/reciprocity needed, unlike every other Asian country in the set), with a new April 2026 Form 22 disclosure requirement (transparency, not a restriction). No retirement visa exists (confirmed, unlike Philippines/Malaysia), and the Digital Nomad Visa is explicitly non-renewable at 6 months (contrasted against Malaysia's 24-month DE Rantau), while the HSP points-based visa offers PR in as little as 1 year at 80 points. Nav dropdown propagated to all 119 sitewide files carrying `#countries-menu`, including all 11 prior countries' own pages. sitemap.xml updated with 10 new Japan URLs (137 total `<url>` entries now).

## Current State (07/15/2026, session 8)
- 11th country added: `cambodia/` (10 pages: hub, filing, FEIE, no-treaty/no-totalization, digital nomad & remote worker status, retiring (ER visa), checklist, teachers, property ownership (strata title), NGO & aid sector workers). Cambodia's standout distinctive angles: a dollarized economy (most transactions in USD), a genuine 2026 enforcement tightening on remote work, self-employed foreigners physically working in Cambodia now clearly need a work permit regardless of where clients are based, the old visa-run/cash-in-hand freelance model is riskier now, covered on its own dedicated page. Property ownership's real risk isn't the 70% foreign ownership cap on strata-titled condos (standard pattern), it's the hard title vs. soft title distinction, soft title (commune-level registration only) is a genuine trap, not a technicality. A large NGO/aid-sector expat population in Phnom Penh earned its own page distinguishing standard NGO employees (normal FEIE rules) from volunteers/diplomats/US-government staff (distinct exemptions, FEIE often doesn't apply to government wages) and covering per diem/allowance tax treatment. Teachers page flags the cash-in-hand contract risk specific to Cambodia's language-center market. Retiring page covers the ER visa extension (age 55+, renewed in 1/3/6/12-month increments, shorter cycles than Malaysia's MM2H or Philippines' SRRV). Nav dropdown propagated to all sitewide files carrying `#countries-menu`, including all 10 prior countries' own pages. sitemap.xml updated with 10 new Cambodia URLs (127 total `<url>` entries now).

## Current State (07/15/2026, session 7)
- 10th country added: `taiwan/` (10 pages: hub, filing, FEIE vs FTC, pending tax agreement & no totalization, Gold Card, retiring, checklist, teachers/Bilingual 2030, property ownership (reciprocity), semiconductor & tech workers). Taiwan is the largest US trading partner with no tax treaty, ever, but genuinely distinct from Vietnam's stalled-since-2015 situation: the House passed the US-Taiwan Expedited Double-Tax Relief Act 423-1 and Treasury announced formal negotiations, real recent bipartisan momentum, covered on its own dedicated page rather than folded into a generic no-treaty page. Taiwan's 40% top bracket (reached above ~$170k USD) is genuinely high like Australia's, so this is the third country (after Australia) where FEIE vs FTC modeling matters for real, not just Gulf-style FEIE-only or high-tax FTC-only patterns. Distinctive angles: the Gold Card (4-in-1 visa: residence + open work permit + ARC + re-entry, plus a real 50% local tax deduction for 3 years), genuine reciprocity-based freehold property ownership (unlike every other Asian country's condo-cap/leasehold restrictions), and a semiconductor/tech-worker page covering RSU/equity sourcing complexity given TSMC's dominance, the playbook's "dominant local industry" niche slot. Teachers page covers Taiwan's genuinely government-funded Bilingual 2030 recruitment policy ($361M invested, target of 4,600 foreign teachers by 2030). Nav dropdown propagated to all 99 sitewide files carrying `#countries-menu`, including all 9 prior countries' own pages. sitemap.xml updated with 10 new Taiwan URLs (117 total `<url>` entries now).

## Current State (07/15/2026, session 6)
- 9th country added: `indonesia/` (10 pages: hub, filing, FEIE, tax treaty & no totalization, 4-year foreign skill tax exemption, retiring, checklist, teachers, property ownership (Hak Pakai/leasehold), Second Home Visa & Remote Worker KITAS). Indonesia is the second country in our coverage (after Philippines) with a genuine, still-active US tax treaty (1988), but still no Totalization Agreement, same self-employment tax trap as everywhere else. Its standout distinctive feature is the 4-year foreign skill tax exemption: qualifying tech/science/math professionals get territorial-style (Indonesian-sourced-only) tax treatment for their first 4 years as a resident, a narrow, fixed-term carve-out unlike Malaysia's broader territorial system. Property is Hak Pakai (right-to-use, requires KITAS/KITAP) or leasehold only, no freehold for foreigners, with an explicit warning against illegal nominee-ownership schemes that persist in expat forums. Indonesia has no official "digital nomad visa" by name but has two genuine legal long-stay routes covered together on one page: the 10-year wealth-based Second Home Visa and the E33G Remote Worker KITAS, both barring local Indonesian employment/income. Nav dropdown propagated to all 89 sitewide files carrying `#countries-menu`, including all 8 prior countries' own pages. sitemap.xml updated with 10 new Indonesia URLs (107 total `<url>` entries now).

## Current State (07/15/2026, session 5)
- 8th country added: `malaysia/` (12 pages, the deepest country build so far, user explicitly approved 8-12 pages given how much genuinely distinct material Malaysia has). Pages: hub, filing, FEIE, no-treaty/no-totalization, territorial tax & foreign income remittance rule, MM2H visa (Silver/Gold/Platinum/SEZ tiers), retiring, 2026 checklist, teachers, property ownership (2026 8% foreign buyer stamp duty), DE Rantau digital nomad visa, and Labuan offshore company & GILTI. Malaysia's standout issue is its territorial tax system: only Malaysia-sourced income (plus remitted foreign income since a 2024 rule change) is taxed locally, with a transitional individual exemption on remitted foreign income running through December 31, 2026, a real planning deadline referenced across multiple pages (retiring, MM2H, the territorial-tax page itself). Labuan's 3% offshore company regime creates a GILTI trap for American owners (minimal tangible assets means minimal QBAI shelter, so nearly all profit becomes GILTI-eligible) fixed via a Check-the-Box election (Form 8832), a distinct business-structuring angle from Vietnam's GILTI coverage. DE Rantau is Malaysia's genuinely legitimate, government-run digital nomad visa, contrasted explicitly against Vietnam's illegal-gray-zone and the Philippines' unresolved-reciprocity situations. Nav dropdown propagated to all 77 sitewide files carrying `#countries-menu`, including all 7 prior countries' own pages. sitemap.xml updated with 12 new Malaysia URLs (97 total `<url>` entries now).

## Current State (07/15/2026, session 4)
- 7th country added: `uae/` (10 pages, hub + filing + FEIE + no-treaty/no-totalization + corporate tax & GILTI + retiring + checklist + teachers + Golden Visa + defense contractors). UAE is Gulf-pattern like Oman/Saudi (no income tax, no treaty, no totalization, FEIE-primary, End of Service Gratuity), but with genuinely distinct angles none of the other 3 Gulf-adjacent pages have: the UAE's 2023 federal corporate tax (9% mainland, 0% for Qualifying Free Zone Persons) combined with 2026's tightened GILTI rules (the physical-assets tax-free buffer was removed) means a 0% local rate on a free zone business no longer implies a 0% US bill for the owner, that got a dedicated Corporate Tax & GILTI page. Also distinctive: fully open freehold real estate for foreigners (100% ownership, 60+ Dubai zones, no residency required, unlike every other country's condo-cap/leasehold restrictions), and the Golden Visa's employer-independent 10-year residency (folded into its own page, distinct from Saudi's Premium Residency treatment since UAE's freelance/property qualifying pathways differ). Defense Contractors page covers Al Dhafra Air Base (long-standing USAF presence near Abu Dhabi), following the same Combat-Zone-Exclusion-does-not-apply pattern established for Oman/Saudi. Nav dropdown propagated to all 67 sitewide files carrying `#countries-menu`, including all 6 prior countries' own pages. sitemap.xml updated with 10 new UAE URLs (85 total `<url>` entries now).

## Current State (07/15/2026, session 3)
- 6th country added: `vietnam/` (10 pages, hub + filing + FEIE + no-treaty/no-totalization + digital nomad legal status + retiring + checklist + teachers/ESL + property ownership 30%/50-year lease + business owners & sourcing). Vietnam is distinct from every prior country: a US-Vietnam tax treaty was signed in 2015 but never ratified (functionally no treaty, unlike Philippines' real one), no Totalization Agreement, no dedicated retirement visa (unlike Philippines' SRRV), and remote work on a tourist visa is explicitly illegal under Article 8.2 of Vietnamese immigration law, tightened further by Decree 59/2026's overstay enforcement (16+ day overstays can trigger deportation). That got its own dedicated "Digital Nomad Legal Status" page rather than a soft-pedaled digital-nomad-visa page like Philippines got. Also distinctive: property is condo-only capped at 30% foreign ownership on a 50-year renewable leasehold (stricter than Philippines' 40%/no-term-limit), and Vietnam's manufacturing/sourcing-hub status (supply chains diversifying from China) earned a dedicated business-owner page covering GILTI/Subpart F entity structuring, a niche not needed in any prior country. Vietnam brackets hit 35% at a lower threshold than Philippines but most salaries still land under the FEIE cap, so FEIE remains primary. Nav dropdown propagated to all 57 sitewide files carrying `#countries-menu`, including all 5 prior countries' own pages. sitemap.xml updated with 10 new Vietnam URLs (75 total `<url>` entries now).

## Current State (07/15/2026, session 2)
- 5th country added: `philippines/` (10 pages, hub + filing + FEIE + tax treaty & no totalization + dual citizens & balikbayan + retiring/SRRV + checklist + teachers + condo ownership 40% rule + digital nomad visa). Philippines' standout issue is different from every other country so far: a large share of "American expats" here are actually Filipino-American dual citizens or balikbayan returnees who don't realize citizenship-based US taxation still applies to them, that got its own dedicated page rather than being folded into the hub. Also distinctive: a real 1982 tax treaty exists (unlike Oman/Saudi) but no Totalization Agreement (same SE tax trap as the Gulf countries anyway), foreigners constitutionally cannot own land (condo-only, capped at 40% foreign ownership per building under RA 4726), and the SRRV retirement visa deposit is itself a reportable FBAR/FATCA account many retirees overlook. Because Philippine tax brackets only hit 35% above ~$142k USD, FEIE (not FTC) is the primary tool here, opposite of Australia. Nav dropdown propagated to all 47 sitewide files carrying `#countries-menu`, including all 4 prior countries' own pages this time (Thailand/Oman/Saudi Arabia/Australia). sitemap.xml updated with 10 new Philippines URLs (65 total `<url>` entries now).

## Current State (07/15/2026)
- 4th country added: `australia/` (10 pages, hub + filing + FEIE vs FTC + superannuation + treaty/totalization + retiring + checklist + teachers + defense contractors + FIRB property investment). Australia's own standout issue is superannuation, IRS treats it as a foreign trust, not a qualified plan, driving Forms 3520/3520-A/8621 for most expats, not a low-tax posting like the Gulf countries, so the FEIE-vs-FTC calculus flips (FTC usually wins given 30-45% Australian marginal rates). Nav dropdown propagated to all 37 sitewide files that carry `#countries-menu` (index, legal pages, every Thailand/Oman/Saudi Arabia page); the 2 Thailand pages confirmed to lack a Countries dropdown by design were left untouched, matching playbook Section 4a's documented exception. sitemap.xml updated with 10 new Australia URLs.

## Current State (07/14/2026)
- Site has 3 countries live: Thailand (14 pages), Oman (9 pages), Saudi Arabia (11 pages), all under one nested "Countries" flyout dropdown in the desktop nav. Mobile no longer has a separate flat link list: `.nav-links` (the same markup as desktop) is now the mobile menu, a full-screen accordion overlay below 1024px, reusing the exact same `#countries-menu` / `.nav-dropdown-sub` markup as desktop. The old `#nav-mobile-menu` block was fully retired site-wide this session; if you ever see one on an old/archived page copy, delete it.
- Shared nav CSS/JS lives only in `css/global.css` / `js/global.js`. `css/pages.css` (loaded after global.css on the 8 `/pages/*.html` articles) used to carry its own competing copy of every nav rule; that duplicate was deleted this session, never re-add nav rules there.
- Mobile layout fixes now baked into `css/global.css` sitewide: floated/grid images collapse to full-width, `.callout-box` inline `160px` padding is clamped down, footer stacks correctly (`.footer-top-inner`'s `grid-auto-flow: column` was the actual bug, not `grid-template-columns`), hero photo hidden + not even downloaded on mobile via `<picture>`, `#nav.scrolled`'s `backdrop-filter` no longer traps `.nav-links`' fixed positioning while the menu is open (backdrop-filter on an ancestor makes it a containing block for `position: fixed` descendants), and every `[id]` now has `scroll-margin-top` so `#anchor` links don't land behind the fixed nav bar.
- Cache-Control on `/css/*` and `/js/*` is 10 minutes (`_headers`), so every shared-file edit needs its `?v=N` query string bumped across all pages in the same commit or the fix won't show for up to 10 min after deploy.
- A one-shot playbook for adding future countries lives at `.md/playbook-new-country.md` (gitignored, not deployed, local reference only), updated this session to match the current nav/mobile system. Read it before adding country #4.
- `js/chaty-widget.js`: floating bottom-left contact widget (Phone/Email/Facebook/Messenger), ported from a sibling project and rebranded. Labels adapt color to whatever's behind them at runtime (near-white on dark sections, brand blue on light sections).
- Legal pages live: `/privacy-policy.html`, `/terms-of-service.html`, `/disclaimer.html` (dark navy background standard, not attorney-reviewed, draft template language). Linked from every page's footer bottom bar.
- Standing rules now baked into every page: no em dashes anywhere, no generic "Get the Full Guide" CTA text (must be content-specific), no "Download Checklist" button (nothing is actually downloadable, and it shouldn't link to email), no duplicate photos within a single page, and `professional-man-formal-bow-tie-confident-*.webp` (a real team member's photo, likely Thomas Carden per the site's schema data) is homepage-only, never reused on article pages.

## Current State (07/13/2026)
- Site complete and Cloudflare Pages deployment-ready on the cloudflare branch (per README).
- SEO implementation done, lighthouse reports present in repo.
- Folder recently active (registry testing variant exists at C:\ZZZZProjects Testing Samples\aitaxadvisors-registry).
- NEW: `oman/` folder built this session — mirrors the `thailand/` folder pattern (no separate index.html; flagship page is the hub). 8 pages, all reusing `css/thailand.css` and existing generic image assets (no Oman-specific photography exists yet).

## Next Steps
- Oman pages reuse generic stock images (flag/currency/professional shots) since no Gulf/Oman-specific photography is in `images/`. Consider sourcing Oman/desert/Muscat imagery if visual differentiation from Thailand pages matters.
- Nav dropdown in `oman/*.html` pages ("Countries") only lists 3 Oman links + Thailand links — could expand to include all 8 Oman pages if desired.
- No `oman/base-template.html` was created (unlike `thailand/base-template.html`) — the generator script effectively replaced it. If future Oman pages are added by hand, either build from an existing Oman page or ask to generate a base-template.
- robots.txt / _headers not touched — only sitemap.xml updated with the 8 new Oman URLs.

## Session Log
- 07/20/2026 (session 12) | Fixed the footer for real this time, found and copied `thailand/*.html`'s existing working implementation instead of continuing to hand-port CSS. See "Current State (07/20/2026, session 12)" above. One commit pushed to `main`. **Still no browser tool available — this was diagnosed from two user-supplied screenshots, not live verification. Confirm rendering before moving to the next country.**
- 07/19/2026 (session 11, round 2) | User feedback pass on the australia work: bigger/scattered body images, unique hero images, homepage-matched footer contact section, font fix. See "Current State (07/19/2026, session 11 continued)" above. One commit pushed to `main`. **Still no browser tool available, verified structurally only.**
- 07/19/2026 (session 11) | Ran the page-remediation-playbook sweep on `australia/*.html` (10 pages), the first of the 11 remaining countries. See "Current State (07/19/2026, session 11)" above. One commit pushed to `main`.
- 07/19/2026 | Full remediation pass on all 13 Thailand pages, see "Current State (07/19/2026, session 10)" above for the complete bug list and fixes. New `.md/page-remediation-playbook.md` written to carry these fixes forward to the other ~130 pages. Three commits pushed to `main`: (1) homepage rebuild + `hp-`/`holistic` rename, (2) preloader removed from every page except the homepage, (3) FAQ corruption fix, Key Topics/specialist/mid-CTA standardization, hero CSS rework, image fixes, checklist grid fix, DTV correction, `md/` → `.md/` rename.
- 07/14/2026 | Fixed reported mobile bugs across all 3 countries (Oman screenshots as the diagnostic starting point, root causes turned out to be global). Real root causes, in the order found: (1) `.callout-box` inline `padding: 0.8rem 160px` squeezing body text to one word per line, (2) `.footer-top-inner`'s `grid-auto-flow: column` never reset on mobile so the footer kept generating columns instead of stacking, (3) `homepage.css`/`thailand.css` cascade order silently re-showing the hero image on country pages despite `homepage.css`'s own hide rule, (4) hero photo still downloaded on mobile at `fetchpriority="high"` despite being CSS-hidden, fixed via `<picture>` + media-gated source, (5) GSAP hero text fade-in (~1.3s delay) forced to instant on mobile for LCP. Then rebuilt the mobile nav entirely: the old `#nav-mobile-menu` was a separate, hand-duplicated flat hub-only link list (no sub-pages reachable on mobile at all, country taps navigated straight to the hub instead of expanding) that had also drifted out of sync content-wise across pages. Deleted it site-wide and made `.nav-links` itself the mobile menu (CMA/chiangmaiambassador's pattern: one markup tree, full-screen accordion below 1024px, `max-height`-based expand/collapse, click handlers with `e.preventDefault()` gated to mobile width). Found and fixed two landmines this surfaced: `css/pages.css` had its own fully independent, older copy of every nav rule that would've silently overridden the fix via cascade order on the 8 `/pages/*.html` articles (deleted the duplicate), and 7 of those same articles had dead inline `<script>` blocks referencing the now-deleted `#nav-mobile-menu` that would've thrown on burger click (removed). Two more scroll-related bugs found after initial "done": `#nav.scrolled`'s `backdrop-filter` was making `#nav` a containing block for its `position: fixed` descendants (`.nav-links`), so the mobile menu rendered behind page content once scrolled past 50px (fixed by dropping the nav bar's own blur while the menu is open); and `#anchor` links landed their target flush behind the fixed nav bar with no offset (fixed with a global `[id] { scroll-margin-top: calc(var(--nav-h) + 1rem) }`). `.md/playbook-new-country.md` updated to match all of the above so the next country build doesn't reintroduce any of it. 12 commits total, all pushed to `main`.
- 07/13/2026 | Built full Oman expat tax content suite (`oman/` folder), mirroring `thailand/` structure. Pages: us-expat-taxes-oman-2026 (hub), filing-us-taxes-from-oman, feie-oman-tax-free-income, no-us-oman-tax-treaty-totalization, oman-2028-income-tax-us-expats, retiring-in-oman-us-tax-planning, us-oman-expat-tax-checklist-2026, us-expat-teachers-tax-oman. Content grounded in user-supplied research on Oman's 2028 personal income tax, lack of US-Oman tax treaty/Totalization Agreement, FEIE mechanics, FBAR/FATCA, PFIC risk, and End of Service Gratuity (EOSG) tax treatment. Updated sitemap.xml with 8 new Oman URLs.
- 07/07/2026 | Retrofit: rules.md and handover.md created, seeded from README.md and folder scan.
